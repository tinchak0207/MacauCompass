import type { PageConfig } from '@tarojs/taro'

const config: PageConfig = {
  navigationBarTitleText: '業務模擬器',
  navigationBarBackgroundColor: '#050505',
  navigationBarTextStyle: 'white'
}

export default config
